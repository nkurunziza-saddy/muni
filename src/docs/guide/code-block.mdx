# Code Block [Example code block]

We will create a simple example code block `tsx file`

```tsx [example.tsx]
import { http, createPublicClient } from "viem";
import { mainnet } from "viem/chains";

const client = createPublicClient({
  chain: mainnet,
  transport: http(),
});
```
